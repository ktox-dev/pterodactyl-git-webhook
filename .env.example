# =============================================================================
# Git Webhook Configuration
# =============================================================================

# Repository directory inside containers where git operations are performed
# This should match the working directory of your containerized applications
REPOS_DIR=/home/container/server-data

# =============================================================================
# Container Configuration
# =============================================================================
# Format: CONTAINER_<container-id>=<branch>
# 
# Supported branches:
# - main: Production containers (pull only, reset local changes)
# - dev:  Development containers (commit, pull, push with submodule handling)
#
# Container IDs should match your Docker container names/IDs

# Production container (main branch)
CONTAINER_34bee3f5-fb2b-4bab-b45e-c303b1d15137=main

# Development containers (dev branch)
CONTAINER_fbb6360b-1f8f-4768-a39e-340daf0eac6f=dev
CONTAINER_51c6374c-c9ff-49bb-90b8-c68d1326fabe=dev

# =============================================================================
# Submodules Configuration
# =============================================================================
# Format: SUBMODULE_<container-id>_<name>=<path>:<branch>
#
# Where:
# - container-id: Must match a container ID defined above
# - name: Unique identifier for the submodule (can be any descriptive name)
# - path: Relative path to the submodule from REPOS_DIR
# - branch: Git branch to use for this submodule
#
# Note: Submodules are only processed for containers with branch "dev"

# Cars submodule for production container
SUBMODULE_34bee3f5-fb2b-4bab-b45e-c303b1d15137_CARS=resources/[VL_Scripts]/[Cars]:main

# Cars submodule for development containers
SUBMODULE_fbb6360b-1f8f-4768-a39e-340daf0eac6f_CARS=resources/[VL_Scripts]/[Cars]:dev
SUBMODULE_51c6374c-c9ff-49bb-90b8-c68d1326fabe_CARS=resources/[VL_Scripts]/[Cars]:dev

# Example: Additional submodules
# SUBMODULE_fbb6360b-1f8f-4768-a39e-340daf0eac6f_WEAPONS=resources/[VL_Scripts]/[Weapons]:dev
# SUBMODULE_fbb6360b-1f8f-4768-a39e-340daf0eac6f_UI=resources/[UI]/[Components]:main

# =============================================================================
# Optional Configuration (uncomment to override defaults)
# =============================================================================

# Flask Server Configuration
FLASK_HOST=0.0.0.0
FLASK_PORT=5000
FLASK_DEBUG=false

# Logging Configuration
LOG_LEVEL=INFO
LOG_FILE=webhook.log

# External API Configuration
GITHUB_API_TIMEOUT=10

# Git User Configuration (used for commits)
# These settings will be automatically configured in each container before making commits
# This prevents the "Author identity unknown" error seen in Git operations
GIT_USER_NAME=Git Webhook Bot
GIT_USER_EMAIL=webhook@example.com

# Health Check Endpoint Configuration
# Set to false to disable the /health endpoint (useful for security)
HEALTH_CHECK_ENABLED=true
